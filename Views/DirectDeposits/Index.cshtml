@model IEnumerable<Alliance_for_Life.Models.DirectDeposits>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IsCheked)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IsCheked)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DirectDepositsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DirectDepositsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DirectDepositsId })
        </td>
    </tr>
}

</table>
